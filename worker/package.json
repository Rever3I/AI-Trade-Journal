{
  "name": "ai-trade-journal-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate": "wrangler d1 execute ai-trade-journal-db --local --file=./src/db/schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.99.0",
    "vitest": "^2.1.8"
  }
}
